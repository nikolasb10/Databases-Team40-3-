<!DOCTYPE html>
<html lang="en">
<head>
  <title>Projects for each researcher</title>
</head>
<body>
    <div class="table-data">
<h2>Projects for each researcher</h2>
    <br>
    <table border="1">
        <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Project id</th>
            <th>Title</th>
        </tr>

        <%
        if(resData.length!=0){
        resData.forEach(function(data){
        %>
        <tr>
            <td><%=data.first_name %></td>
            <td><%=data.last_name %></td>
            <td><%=data.project_id %></td>
            <td><%=data.title %></td>
        </tr>
        <%  }) %>
        <% } else{ %>
            <tr>
                <td colspan="7">No Data Found</td>
            </tr>
        <% } %>
    </table>
    </div>
</body>
</html>
